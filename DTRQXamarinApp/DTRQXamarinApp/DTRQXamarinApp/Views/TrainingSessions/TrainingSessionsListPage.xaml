<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="DTRQXamarinApp.Views.TrainingSessions.TrainingSessionsListPage"
             Title="{Binding Title}">
    
    <ListView ItemsSource="{Binding Items}"  CachingStrategy="RecycleElement" SeparatorVisibility="Default">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <!--<ViewCell.ContextActions>
                        <MenuItem Command="RegisterCommand" CommandParameter="{Binding .}"
                                  Text="Register"/>
                    </ViewCell.ContextActions>-->
                    <StackLayout Orientation="Horizontal" Padding="3">
                        <Label Text="{Binding Date}" FontAttributes="Bold" />
                        <Label Text="{Binding AvailableSeat}" HorizontalOptions="EndAndExpand"/>
                        <Button Command="{Binding RegisterCommand}" Text=" + S'inscrire"
                                CommandParameter="Id"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>