<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="DTRQXamarinApp.Views.TrainingSessions.TrainingSessionsListPage"
             Title="{Binding Title}">
    
    <ListView ItemsSource="{Binding Items}" x:Name="LstItems"  CachingStrategy="RecycleElement" SeparatorVisibility="Default">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal" Padding="3">
                        <Label Text="Session : " FontAttributes="Bold" />
                        <Label Text="{Binding Date}" FontAttributes="Bold" />
                        <!--<Image Source=" "/>-->
                        <Label Text="{Binding AvailableSeat}" HorizontalOptions="EndAndExpand"/>
                        <Button Text=" S'inscrire"
                                Command="{Binding Path=BindingContext.Register, Source={x:Reference LstItems}}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="#FF669900"
                                TextColor="White"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>